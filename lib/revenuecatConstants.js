/**
 * RevenueCat entitlement identifier — create an entitlement with this exact
 * identifier in the RevenueCat dashboard and attach your subscription products.
 */
export const REVENUECAT_ENTITLEMENT_PRO = "ZeroGasto Pro";

/**
 * Store subscription product identifiers (App Store Connect / Play Console).
 * Add the same IDs as products in RevenueCat and attach them to the offering
 * (e.g. default offering) as packages.
 */
export const REVENUECAT_PRODUCT_IDS = Object.freeze({
  MONTHLY: "monthly",
});
